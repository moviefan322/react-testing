[{"content":"import { screen, render, within } from '@testing-library/react';\n\nfunction FormData() {\n  return (\n    <div>\n      <button>Go Back</button>\n      <form aria-label=\"form\">\n        <button>Save</button>\n        <button>Cancel</button>\n      </form>\n    </div>\n  );\n}\n\nrender(<FormData />);","type":"code","id":"9gmvf"},{"content":"function toContainRole(container, role, quantity = 1) {\n  const elements = within(container).queryAllByRole(role);\n\n  if (elements.length === quantity) {\n    return {\n      pass: true,\n      message: () => 'the container did not contain 2 buttons',\n    };\n  }\n\n  return {\n    pass: false,\n    message: () =>\n      `Expected to find ${quantity} ${role} elements. Found ${elements.length} instead`,\n  };\n}\n\nexpect.extend({ toContainRole });","type":"code","id":"7t9a4"},{"content":"test('the form displays two buttons', () => {\n  render(<FormData />)\n\n  const form = screen.getByRole('form')\n\n  expect(form).toContainRole('button', 2)\n})","type":"code","id":"is6zw"}]